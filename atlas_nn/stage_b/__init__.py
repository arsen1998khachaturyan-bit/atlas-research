"""Stage B (mission section 9): does *training* create compressible
structure in weight tensors, compared to the same architecture at random
initialization?

Requires torch (CPU). Kept in its own subpackage so Stage A
(`atlas_nn.synthetic`, `atlas_nn.baselines`, `atlas_nn.structural`) has no
torch dependency.
"""
